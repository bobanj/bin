My Custom bash settings, alias definitions, functions...
--------------------------------------------------------

This works on a Mac. YMMV.

Import:

    if [ -f ~/bin/.bashrc ]; then
      . ~/bin/.bashrc
    fi

